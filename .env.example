# Variáveis de Ambiente

## Desenvolvimento Local

Crie `.env.development`:

```env
# API Backend
REACT_APP_API_URL=http://localhost:3001/api
REACT_APP_GOOGLE_CLIENT_ID=seu_google_client_id.apps.googleusercontent.com

# Servidor Web
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# Banco de Dados
DB_HOST=localhost
DB_PORT=5432
DB_NAME=filesfy
DB_USER=postgres
DB_PASSWORD=sua_senha

# JWT
JWT_SECRET=sua_chave_secreta_desenvolvimento
JWT_EXPIRATION=7d

# Email (opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu_email@gmail.com
SMTP_PASSWORD=sua_senha_app
```

## Produção

Crie `.env.production`:

```env
# API Backend
REACT_APP_API_URL=https://api.filesfy.com
REACT_APP_GOOGLE_CLIENT_ID=seu_google_client_id_prod.apps.googleusercontent.com

# Servidor Web
PORT=3000
NODE_ENV=production
CORS_ORIGIN=https://www.filesfy.com

# Banco de Dados
DB_HOST=seu_host_db_produção
DB_PORT=5432
DB_NAME=filesfy_prod
DB_USER=filesfy_user
DB_PASSWORD=senha_forte_aleatória

# JWT
JWT_SECRET=chave_secreta_super_segura_aleatória
JWT_EXPIRATION=30d

# Stripe (Pagamentos)
STRIPE_SECRET_KEY=sk_live_...
STRIPE_PUBLISHABLE_KEY=pk_live_...

# Email
SMTP_HOST=smtp.seuservidor.com
SMTP_PORT=587
SMTP_USER=noreply@filesfy.com
SMTP_PASSWORD=senha_forte
SMTP_FROM=Filesfy <noreply@filesfy.com>

# Logs
LOG_LEVEL=info
SENTRY_DSN=https://seu-sentry-dsn@sentry.io/...
```

## Staging (Pré-produção)

Crie `.env.staging`:

```env
REACT_APP_API_URL=https://api-staging.filesfy.com
REACT_APP_GOOGLE_CLIENT_ID=seu_google_client_id_staging.apps.googleusercontent.com
NODE_ENV=staging
CORS_ORIGIN=https://staging.filesfy.com
DB_NAME=filesfy_staging
```

## Como Usar

### Desenvolvimento
```bash
cp .env.development .env
npm run start:web
```

### Produção
```bash
cp .env.production .env
npm run web:dev
```

### Variáveis Importantes

| Variável | Descrição | Exemplo |
|----------|-----------|---------|
| `REACT_APP_API_URL` | URL da API backend | `http://localhost:3001/api` |
| `PORT` | Porta do servidor web | `3000` |
| `NODE_ENV` | Ambiente | `development`, `production` |
| `JWT_SECRET` | Chave para assinar JWT | Qualquer string longa |
| `STRIPE_SECRET_KEY` | Chave Stripe (se usar pagamento) | `sk_live_...` |
| `SMTP_HOST` | Servidor de email | `smtp.gmail.com` |

## ⚠️ Segurança

- ✅ NUNCA commite `.env` no Git
- ✅ Use secrets do seu servidor para variáveis sensíveis
- ✅ Gere senhas fortes e únicas
- ✅ Rotação de chaves regularmente
- ✅ Audite logs de acesso

## 🔐 Gerar Chaves Seguras

```bash
# Linux/Mac
openssl rand -hex 32

# Windows PowerShell
[System.Security.Cryptography.RNGCryptoServiceProvider]::GetBytes(16) | %{"{0:X2}" -f $_} -join ""
```
